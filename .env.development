# Dev only: the app talks to the containerized PocketBase.
# In production the PWA is served by PocketBase itself, so this is unset and the
# client defaults to same-origin ("/").
VITE_PB_URL=http://localhost:8090
